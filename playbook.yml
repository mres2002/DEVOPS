---
- name: Install nginx with virtual Hosts
  hosts: webservers
  remote_user: vboxuser
  become: true
  become_method: sudo
  gather_facts: no

  vars:
    sites:
    - "etis.com"
    - "fizfak.com"
    - "mehmat.ru"

  roles:
  - nginx-vhosts
